package com.morpheusdata.model.event;

public interface IEventScope {
	// NOTE: This interface exists as a stop gap to move the EventScope enum OUT of GenericIntegrationProvider.SubscriberEventFacet
	// but still allow it to function while being deprecated.
}
